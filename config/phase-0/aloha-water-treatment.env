# Aloha Water Treatment Simulator configuration
# Copy to /etc/aloha-water-treatment.env on the deployed VM to override defaults.

# Modbus TCP port used by both PLC server and HMI client.
# Port 502 (standard Modbus) requires running the service as root.
MODBUS_PORT=5020
