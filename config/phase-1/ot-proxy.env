# Optional host identity configuration for the OT-Proxy VM.

OT_HOSTNAME=ot-proxy

# Default OT-Proxy port is 5020, but can be overridden if needed (e.g. to avoid conflicts in testing environments)
# OT_PROXY_PORT=502