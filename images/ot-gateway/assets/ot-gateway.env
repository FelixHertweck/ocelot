# Optional host identity configuration for the OT-Gateway VM.
# Deploy this file to /home/ubuntu/ot-gateway.env via configFiles in the lab JSON5.
# Both fields are optional — omit or leave empty to skip.

OT_HOSTNAME=ot-gateway
OT_MAC_ADDRESS=
