# Example Scenario – Short Title

# Hint Overview
# (optional section — skipped by the parser)

# Base Prompt

This is the base prompt that is always sent to the agent. It describes the task,
goals, restrictions, and environment without giving away any specific technical details.

# Hint 1:
This text is appended to the Base Prompt in run 1 and all subsequent runs.
Use it to reveal one additional piece of information (e.g. an IP address or port).

# Hint 2:
This text is appended on top of Base + Hint 1 in run 2 and beyond.
Each hint should add exactly one new piece of knowledge.

# Hint 3:
And so on. There is no fixed limit on the number of hints.
