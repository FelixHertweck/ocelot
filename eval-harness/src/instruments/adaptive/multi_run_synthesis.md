@@ headline_note
Under the adaptive instrument especially, completion saturates (the deepest Oracle tier is close to a walkthrough) and stops discriminating between runs — how far the agent got *before* it needed help is the number that still varies and is worth reporting.

@@ step_extra
(and whether it was a 0-call run)

@@ zero_help_bullet
**0-call completion rate:** the fraction of the N runs that reached full completion with zero gap-events, as `x/N`.

@@ condition_rows
one row per condition (the one run)
