@@ headline_note

@@ step_extra

@@ zero_help_bullet
**Base completion rate:** the fraction of the N runs that reached full completion at the Base dose (no added knowledge), as `x/N`.

@@ condition_rows
one row per condition (each dose)
